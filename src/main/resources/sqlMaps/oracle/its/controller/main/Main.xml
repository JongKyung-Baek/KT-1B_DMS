<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sql.Main">
	<select id="selectSessionTime" resultType="Integer">
		SELECT session_time
		FROM session_config
		WHERE id = 1
	</select>

	<update id="updateSessionTime">
		UPDATE session_config
		SET session_time = #{sessionTimeout}
		WHERE id = 1
	</update>
</mapper>
